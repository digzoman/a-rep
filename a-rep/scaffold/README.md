# A Rep scaffolds

`agent-repo/` is the canonical V1.1 filesystem scaffold for a private persistent-agent repository.

New agents should be bootstrapped from this structure. Existing agents should be patched conservatively, preserving agent-specific work, procedures, scratch material, and configuration.

The scaffold is intentionally structural rather than application-specific. It defines where categories of material belong without prescribing the content of real work.
