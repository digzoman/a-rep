# config

Non-secret configuration for the persistent agent and A Rep runtime.

`arep.env` is the normal V1 runtime configuration. It may contain machine paths, cadence, execution-driver selection, and local runtime-state paths.

Do not store credentials, API keys, tokens, passwords, or other secrets here.
